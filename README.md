# Memsplit

This is a Napari plugin developed to perform automated classification of semantic membrane segmentations of scores volume generated by membrain-seg or any segmentation that contains a probability map(attach the hyperlink for membrain-seg). 

Additionally this tool contain several tools to remove for artifacts in the Z-axis, easily put the instances generated from the watershed into different categories. Perform connected components. 

